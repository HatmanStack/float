# Backend Testing Workflow
# Runs linting, type checking, and tests on every push

name: Backend Tests

on: push

jobs:
  test:
    name: Lint, Type Check, and Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # Python setup
      - name: Set up Python 3.13
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Install uv
        run: curl -LsSf https://astral.sh/uv/install.sh | sh

      - name: Add uv to PATH
        run: echo "$HOME/.local/bin" >> $GITHUB_PATH

      - name: Cache Python dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache/uv
          key: ${{ runner.os }}-uv-${{ hashFiles('backend/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-uv-

      - name: Install Python dependencies
        run: |
          cd backend
          uv pip install --system -r requirements.txt -r requirements-dev.txt

      # Linting
      - name: Lint with ruff
        run: |
          cd backend
          ruff check src/ tests/

      - name: Check formatting with black
        run: |
          cd backend
          black --check src/ tests/

      # Type checking
      - name: Type check with mypy
        run: |
          cd backend
          mypy src/

      # Tests
      - name: Run unit tests
        run: |
          cd backend
          pytest tests/unit/ -v

# Integration tests disabled - requires AWS credentials or API keys
# To enable: set up secrets in GitHub Settings, then uncomment below
#
# - name: Run integration tests
#   env:
#     G_KEY_TEST: ${{ secrets.G_KEY_TEST }}
#     OPENAI_API_KEY_TEST: ${{ secrets.OPENAI_API_KEY_TEST }}
#   run: |
#     cd backend
#     pytest tests/integration/ -v
